# Testing Notes

Testing details will expand as features are implemented.

Phase 1 smoke checks:

- `php artisan about`
- `php artisan route:list`
- `npm run build`
- Browser check for `/`
- Browser check for `/up`
